# what i can do to make my project better...?
#  -- add the email verfiction 
#  -- add my message pop ups
#  -- add more features to about page 
#  -- contact page ( get it working / maybe get a better)
#  -- as time goes on a better design
#  -- login better layout (works)
#  -- forget password stoped working fix it
#  -- guest mode - view button should display product info ,add product images , something wrong with cart logo
#  -- register better layout (works)
#  -- store , permisson to grant access must be logged in , display products and view should display product info , search function
#  -- get cart , checkout and payment sorted
# -- make sure functions works properly





<!-- SIGNUP

<!DOCTYPE html>
<html lang="en" dir="ltr">
 
    <head>
        <meta charset="UTF-8">
        <title>Green Gate Website</title>
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
               {% load static%}
        <link rel="stylesheet" href="{% static 'register5.css' %}">
    </head>
    <body>
        <nav>
            <input type="checkbox" id="check">
            <label for="check" class="checkbtn">
                <i class="fas fa-bars"></i>
            </label>
            
            <label class="logo">Green Gate <img src="{% static 'shopping-cart.png' %}"></label>
            <ul>
                <li><a class="active" href={% url 'home'%} "index.html">Home</a></li>
                <li><a href={% url 'about'%} "About.html">About Us</a></li>
                <li><a href={% url 'contact'%} "Contact.html">Contact</a></li>
                <li><a href={% url 'login'%} "Login.html">Login</a></li>
            </ul>
        </nav>

    

        <br><br><br>
        <div class="center"> 
          <br>
            <h2>Customer Registeration</h2>
            <br>
            <form action="" method="post">   
              {% csrf_token %}
            <div class="txt_field">
                <input type="text" name="Username" id="Username" required="required">
                <label>Create a username</label>
                <br>
              </div>
              
             <div class="txt_field">
                <input type="email" name="Email address" id="Email address" required="required">
                <label>Enter Valid Email address</label>
                <br>
              </div>

              <div class="txt_field">
                <input type="password" name="pass1" id="pass1" required="required">
                <label> Create password</label>
                <br>
              </div>

              <div class="txt_field">
                <input type="password" name="pass2" id="pass2" required="required">
                <label>Confirm password</label>
                <br>
              </div>
              <br>
             <input type="submit" value="Register">
             <br><br><br>
             <div class="login_link"> <p>Already a memeber ? <a href ={% url 'login'%} "Login.html">Login</a></p>
             </div>  
             <div class="or_link"><p>OR</p></div> 
             <div class="guest_link"> <p>Continue as Guest...  <a href ={% url 'guest'%} "Guest.html" >Guest Mode</a></p>

              {% if username %}
              <div class="alert alert-danger alert-dismissible">
                  <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                  {{ username }}
              </div>
              <br>
          {% endif %}

           </form>
       </div>
    </body>
    </html>    -->



    <!-- views.py Register
    def RegisterPage(request):
    if request.method=='POST':
     uname=request.POST.get('username')   
     email=request.POST.get('Email address') 
     pass1=request.POST.get('pass1') 
     pass2=request.POST.get('pass2')
     
    if pass1!=pass2:
         return HttpResponse('Your password and conform password are not the Same!!')
    else:
            if len(pass1) < 8:
                msg = "Please create a stronger pass the length is too short!"
                messages.info(request, msg)
                return HttpResponseRedirect(request.path)     
            if pass1 != pass2:
                msg = "Please make sure that both passwords are the same !"
                messages.error(request, msg)
                return HttpResponseRedirect(request.path)
            else:
                if uname == '':
                    msg = "Please make sure that you have created a username !"
                    messages.info(request, msg)
                    return HttpResponseRedirect(request.path)            

                else:
                    if email == '':
                        msg = "Please ensure that you have entered your email !"
                        messages.info(request, msg)
                        return HttpResponseRedirect(request.path)   
            
                    else:
                        if pass1 == '':
                            msg = "Please ensure that you have created a password !"
                            messages.info(request, msg)
                            return HttpResponseRedirect(request.path)            
            
    if User.objects.filter(username=uname).exists():
                msg = f"{uname} already exists!"
                messages.warning(request, msg)
                return HttpResponseRedirect(request.path)
            
    if User.objects.filter(email=email).exists():
                msg = f"{email} already exists!"
                messages.warning(request, msg)
                return HttpResponseRedirect(request.path)
    else:
                User.objects.create_user(uname, email, pass1)
                new_user = authenticate(request, username=uname, password=pass1)
                if new_user is not None:
                    login(request, new_user)
                    msg = f"{uname} Thank You for Registering , You are now Login"
                    messages.success(request, msg) 
                    return HttpResponseRedirect(request.path) -->



<!-- LOGIN 


<!DOCTYPE html>
<html lang="en" dir="ltr">
 
    <head>
        <meta charset="UTF-8">
        <title>Green Gate Website</title>
        <meta name="viewport" content="width=device-width,initial-scale=1.0">
        {% load static%}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
        <link rel="stylesheet" href="{% static 'loginn.css' %}">
    </head>
    <body>
      <nav>
        <input type="checkbox" id="check">
        <label for="check" class="checkbtn">
            <i class="fas fa-bars"></i>
        </label>
        
        <label class="logo">Green Gate <img src="{% static 'shopping-cart.png' %}"></label>
        <ul>
            <li><a class="active" href={% url 'home'%} "index.html">Home</a></li>
            <li><a href={% url 'about'%} "About.html">About Us</a></li>
            <li><a href={% url 'contact'%} "Contact.html">Contact</a></li>
           <li><a href={% url 'register'%} "Signup.html">SignUp</a></li>
        </ul>
    </nav>
    {% comment %} {% if messages %}
    <ul class="messages">
        {% for message in messages %}
            <li class="{{ message.tags }}">
                {{ message|safe }}
                <a onclick="window.location.reload()"; href="#" class="close">X</a>
        {% endfor %}
    </ul>
{% endif %} {% endcomment %}

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>

            <div class="center"> 
              <br>
             <h2>Customer Login</h2>
             <br>
             <form action="" method="post">  
                {% csrf_token %} 
             <div class="txt_field">
                 <input type="text" name="username" required="required">
                 <label>Username</label>
                 <br>
               </div>
               
               <div class="txt_field">
                <input type="password"name="pass" required="required">
                <label>password</label>
                <br>
              </div>
              <br>
              <input type="submit" value="Login">
              <br><br>
              
              
              <div class="signup_link"><p>Not a memeber yet?  <a href ={% url 'register'%} "Signup.html" >Register</a></div>
                <div class="signup_link"><p><a href={% url 'reset_password'%} "password_reset_form.html">Forget Password</a></p></div>
              <div class="or_link"><p>OR</p></div> 
              <div class="guest_link"> <p>Continue as Guest...  <a href ={% url 'guest'%} "Guest.html" >Guest Mode</a></p>
             
               /..
            </form>
        </div>
            </body>
    </html>   -->